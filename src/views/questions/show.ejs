<!DOCTYPE html>
<html>
  <head>
    <% include ../static/partials/head.ejs %>
  </head>
  <body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">
      <br>

      <h5>Subject : <%= question.subject %></h5>

      <p><%= question.body %></p>

      <a href="/categories/<%= question.categoryId %>/questions/<%= question.id %>/edit" class="btn btn-warning">Edit</a>

        <form style="display:initial" action="/categories/<%= question.categoryId %>/questions/<%= question.id %>/destroy" method="post">
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
    </main>

    <% include ../static/partials/baseScripts.ejs %>

  </body>
</html>
